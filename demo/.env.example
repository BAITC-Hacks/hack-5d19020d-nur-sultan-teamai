# Copy to demo/.env or use repo-root .env — never commit secrets.
OPENAI_API_KEY=
NVIDIA_API_KEY=
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
WIND_DEMO_LLM_MODE=auto
# auto | openai | nvidia | mock
