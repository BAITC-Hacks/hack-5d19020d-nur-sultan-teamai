# External blockers

1. The two Maps short links must be resolved to the coordinates of each POI/marker. Network
   access to the links returned HTTP 403 in the current environment.
2. The original two XLSX workbooks and task PDF are not present in the repository. T00 input
   hashes and T03 real-data ingestion therefore cannot yet be produced.
3. The SCADA timezone, timestamp semantics, and publication delay require owner confirmation
   before strict mode can be enabled.

These blockers do not prevent offline software work. They do prevent claims of strict replay or
real model quality.
